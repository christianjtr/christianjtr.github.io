---
interface Props {
    title: string;
    class?: string;
}

const { title, class: className } = Astro.props;
---

<section class={className}>
    <h1 class="text-2xl font-bold">{title}</h1>
    <slot/>
</section>